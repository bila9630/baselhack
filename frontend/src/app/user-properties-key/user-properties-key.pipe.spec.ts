import { UserPropertiesKeyPipe } from './user-properties-key.pipe';

describe('UserPropertiesKeyPipe', () => {
  it('create an instance', () => {
    const pipe = new UserPropertiesKeyPipe();
    expect(pipe).toBeTruthy();
  });
});
